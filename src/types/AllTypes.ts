export type aiMessagesType = {
    aiResponse:string,
    timestamp:number,
}

export type messageType = {
    type:string,
    text:string,
    timestamp:number,
}

export type pdf ={
    pdfId:string,
    pdfName:string
}
